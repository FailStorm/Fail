using UnityEngine;
using System.Collections;

public class Nest : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}



/*nest position x, y
nest.transform.position = new vector2 (x, y), if not, do z too
bird: 
get position of the nest, look camera script
*/